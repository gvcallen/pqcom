#include <EEPROM.h>

const uint8_t vals[5120] = {0};
void setup()
{
    Serial.begin(115200);
    delay(100);
    EEPROM.begin(sizeof (vals));

    EEPROM.writeBytes(0, vals, sizeof (vals));
    EEPROM.commit();
}


void loop()
{
}